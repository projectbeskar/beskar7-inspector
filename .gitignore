# Rust build output
/target/
**/*.rs.bk
# Cargo.lock IS committed for this crate (it builds a binary, not a library).

# Initramfs / image build outputs
build/
dist/
*.img
*.iso
vmlinuz*
initrd*

# Temporary files
*.log
*.tmp
.DS_Store

# IDE
.vscode/
.idea/
*.swp
*.swo
*~

# Test artifacts
test-results/
*.test

# Docker
.dockerignore

# Local development tooling configuration (per-developer, not shared).
# Mirrors the beskar7 repo: the Claude agents, CLAUDE.md, and the
# .claude/context working-memory file are local, not committed.
/.claude/
/CLAUDE.md
